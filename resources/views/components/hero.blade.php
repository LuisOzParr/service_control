@if( $slot != '')
    <section class="hero is-dark">
        <div class="hero-body">
            <div class="container">
                <h1 class="title">
                    {{$slot}}
                </h1>
            </div>
        </div>
    </section>

@endisset